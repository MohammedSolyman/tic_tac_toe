import 'package:flutter/material.dart';

class PortraitGameView extends StatelessWidget {
  const PortraitGameView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('portrait game view'),
      ),
    );
  }
}
