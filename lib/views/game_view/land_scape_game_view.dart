import 'package:flutter/material.dart';

class LandscapeGameView extends StatelessWidget {
  const LandscapeGameView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('land scape game view'),
      ),
    );
  }
}
